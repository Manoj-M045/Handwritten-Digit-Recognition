# Handwritten-Digit-Recognition
Handwritten Digit Recognition is a Computer Vision project that uses the Scikit-Learn Digits dataset and the K-Nearest Neighbors (KNN) algorithm to identify handwritten digits. The project involves data preprocessing, model training, accuracy evaluation, and digit prediction, achieving approximately 98% classification accuracy.
